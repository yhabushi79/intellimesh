---
- name: Remediate TLS Compliance Issue
  hosts: all
  become: yes
  tasks:
    - name: Check files in conf.d
      ansible.builtin.command: ls -la /etc/httpd/conf.d/
      register: conf_d_files

    - name: Print files
      ansible.builtin.debug:
        var: conf_d_files
