---
- name: Remediate TLS Compliance Issue
  hosts: all
  become: yes
  tasks:
    - name: Check error log
      ansible.builtin.command: tail -n 50 /var/log/httpd/error_log
      register: error_log

    - name: Print error log
      ansible.builtin.debug:
        var: error_log
