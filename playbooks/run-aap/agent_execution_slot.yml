---
- name: Remediate TLS Compliance Issue
  hosts: all
  become: yes
  tasks:
    - name: Check demo-ssl.conf
      ansible.builtin.command: cat /etc/httpd/conf.d/demo-ssl.conf
      register: demo_ssl

    - name: Print demo-ssl.conf
      ansible.builtin.debug:
        var: demo_ssl
