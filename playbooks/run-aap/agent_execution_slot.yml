---
- name: Remediate TLS Compliance Issue
  hosts: all
  become: yes
  tasks:
    - name: Check certs
      ansible.builtin.command: ls -laZ /etc/pki/tls/private/demo.key /etc/pki/tls/certs/demo.crt
      register: certs

    - name: Print certs
      ansible.builtin.debug:
        var: certs
