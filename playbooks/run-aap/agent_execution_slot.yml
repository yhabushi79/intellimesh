
# playbooks/run-aap/agent_execution_slot.yml
---
# ┌─────────────────────────────────────────────────────────┐
# │ AGENT EXECUTION SLOT                                     │
# │ Designated workspace for autonomous agent operations.   │
# │ Content is overwritten per-task via GitHub MCP.         │
# │ DO NOT manually edit — agent-managed only.              │
# └─────────────────────────────────────────────────────────┘
- name: "Agent Execution Slot - No Active Task"
  hosts: localhost
  gather_facts: false
  tasks:
    - name: No task loaded
      ansible.builtin.debug:
        msg: "This slot has no active task. An agent must populate it before execution."
      failed_when: true
