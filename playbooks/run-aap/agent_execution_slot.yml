---
- name: Remediate TLS Compliance Issue
  hosts: all
  become: yes
  tasks:
    - name: Check ssl-params.conf
      ansible.builtin.command: cat /etc/httpd/conf.d/ssl-params.conf
      register: ssl_params

    - name: Print ssl-params.conf
      ansible.builtin.debug:
        var: ssl_params
